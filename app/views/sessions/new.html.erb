<!DOCTYPE html>
<html>
<head>
  <title></title>
</head>
<body>
  <% provide(:title, "Log in") %>
  <h1 align="center">로그인</h1><br>

  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <%= form_for(:session, url: login_path) do |f| %>

        <%= f.label '아이디' %>
        <%= f.text_field :login_id, class: 'form-control' %><br>

        <%= f.label '비밀번호' %>
        <%= f.password_field :password, class: 'form-control' %><br>

        <%= f.submit "Log in", class: "btn btn-primary" %><br>
      <% end %><br>

      <p>아이디가 없다면? <%= link_to "회원 가입", new_user_path %></p>
    </div>
  </div>
</body>
</html>
